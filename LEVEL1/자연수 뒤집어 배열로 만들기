class Solution {
    public int[] solution(long num) {
        int len = (int)( Math.log10(num)+1 );
        int[] answer = new int[len];
        for (int i = 0; i < len; i++) {
            answer[i] = (int) num % 10;
            num /= 10;
        }
       
        return answer;
    }
}
